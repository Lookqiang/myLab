# 汉字书写演示 - Hanzi Writer H5 项目

这是一个使用 Hanzi Writer 库创建的汉字书写演示 H5 应用，可以展示汉字的书写动画、提供测试模式等功能。

## 功能特性

- 🎯 **汉字书写动画** - 支持任意汉字的书写动画演示
- ✏️ **测试模式** - 用户可以按照提示练习书写
- 🎨 **美观界面** - 现代化的响应式设计
- 📱 **移动端适配** - 完美支持手机和平板设备
- ⚡ **实时预览** - 输入汉字即可看到书写效果
- 🎮 **示例汉字** - 提供常用汉字快速体验

## 技术栈

- **Hanzi Writer** - 汉字书写动画库
- **Vite** - 现代化前端构建工具
- **原生 JavaScript** - 无框架依赖，轻量高效
- **CSS3** - 现代样式和动画效果

## 快速开始

### 环境要求

- Node.js 16+ 
- npm 或 pnpm

### 安装依赖

```bash
# 使用 npm
npm install

# 或使用 pnpm
pnpm install
```

### 开发模式

```bash
# 启动开发服务器
npm run dev

# 或
pnpm dev
```

访问 http://localhost:3000 查看应用。

### 构建生产版本

```bash
# 构建生产版本
npm run build

# 或
pnpm build
```

### 预览生产版本

```bash
# 预览构建结果
npm run preview

# 或
pnpm preview
```

## 使用说明

### 基本操作

1. **输入汉字** - 在输入框中输入想要演示的汉字
2. **播放动画** - 点击"播放动画"按钮查看书写过程
3. **测试模式** - 点击"测试模式"进行书写练习
4. **重置** - 点击"重置"按钮重新开始

### 高级功能

- **显示/隐藏轮廓** - 控制是否显示汉字轮廓
- **显示/隐藏汉字** - 控制是否显示目标汉字
- **示例汉字** - 点击底部示例汉字快速体验

### 键盘快捷键

- `Enter` - 播放动画
- `Escape` - 重置书写

## 项目结构

```
hanzi-writer/
├── index.html          # 主页面
├── package.json         # 项目配置
├── vite.config.js      # Vite 配置
├── .gitignore         # Git 忽略文件
├── src/
│   └── main.js        # 主要 JavaScript 逻辑
├── styles/
│   └── main.css       # 样式文件
└── public/            # 静态资源目录
```

## 自定义配置

### 修改书写动画参数

在 `src/main.js` 中的 `initWriter` 方法里可以调整：

```javascript
this.writer = HanziWriter.create(this.targetDiv, this.currentChar, {
    width: 300,              // 画布宽度
    height: 300,             // 画布高度
    padding: 5,              // 内边距
    strokeColor: '#333333',  // 笔画颜色
    radicalColor: '#ff6b6b', // 部首颜色
    strokeAnimationSpeed: 1, // 动画速度
    delayBetweenStrokes: 100, // 笔画间延迟
    showOutline: true,       // 显示轮廓
    showCharacter: true      // 显示汉字
});
```

### 添加更多示例汉字

在 `index.html` 中添加更多示例按钮：

```html
<button class="example-char" data-char="新">新</button>
<button class="example-char" data-char="年">年</button>
```

## 浏览器兼容性

- Chrome 60+
- Firefox 60+
- Safari 12+
- Edge 79+

## 开发说明

### 主要类和方法

- `HanziWriterApp` - 主应用类
- `initWriter()` - 初始化书写器
- `animateCharacter()` - 播放动画
- `toggleQuizMode()` - 切换测试模式
- `updateStrokeInfo()` - 更新汉字信息

### 扩展功能

可以通过 `window.HanziWriterUtils` 访问工具函数：

```javascript
// 批量加载汉字
HanziWriterUtils.loadMultipleChars(['永', '福', '爱']);

// 创建汉字网格
HanziWriterUtils.createCharacterGrid(['春', '夏', '秋', '冬'], 'grid-container');
```

## 许可证

MIT License

## 贡献

欢迎提交 Issue 和 Pull Request！

## 相关链接

- [Hanzi Writer 官方文档](https://hanziwriter.org/)
- [Vite 官方文档](https://vitejs.dev/)
- [项目演示地址](#) (部署后填写)