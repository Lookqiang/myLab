# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#
# pre-push:
#   jobs:
#     - name: packages audit
#       tags:
#         - frontend
#         - security
#       run: yarn audit
#
#     - name: gems audit
#       tags:
#         - backend
#         - security
#       run: bundle audit
#
# pre-commit:
#   parallel: true
#   jobs:
#     - run: yarn eslint {staged_files}
#       glob: "*.{js,ts,jsx,tsx}"
#
#     - name: rubocop
#       glob: "*.rb"
#       exclude:
#         - config/application.rb
#         - config/routes.rb
#       run: bundle exec rubocop --force-exclusion {all_files}
#
#     - name: govet
#       files: git ls-files -m
#       glob: "*.go"
#       run: go vet {files}
#
#     - script: "hello.js"
#       runner: node
#
#     - script: "hello.go"
#       runner: go run

pre-commit:
  parallel: true
  commands:
    format:
      run: pnpm prettier --write --ignore-unknown {staged_files}
      glob: "*.{js,ts,jsx,tsx,vue,json,yml,yaml,md,html,css,scss,less,styl}"
    lint-js:
      run: pnpm eslint --fix --cache --no-warn-ignored {staged_files}
      glob: "*.{js,ts,jsx,tsx,vue}"
    lint-style:
      run: pnpm stylelint --fix --allow-empty-input {staged_files}
      glob: "*.{scss,less,styl,html,vue,css}"

post-merge:
  commands:
    install:
      run: pnpm install

commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit $1
